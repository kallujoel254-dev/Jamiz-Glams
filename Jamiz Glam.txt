 <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Jamiz Glam — luxury perfumes and beauty services including makeup, bridal glam, henna brows and turban styling.">
  <title>Jamiz Glam | Discover Luxury. Express Your Elegance.</title>

  <style>
    :root{
      --pink:#d94f86;
      --deep-pink:#a52f61;
      --soft-pink:#fff1f7;
      --black:#171217;
      --white:#ffffff;
      --gold:#c8a45d;
      --gray:#6f6870;
      --light:#faf8fa;
      --border:#eadde4;
      --shadow:0 18px 45px rgba(48,18,35,.10);
      --radius:22px;
    }

    *{box-sizing:border-box;margin:0;padding:0}
    html{scroll-behavior:smooth}
    body{
      font-family:Arial, Helvetica, sans-serif;
      color:var(--black);
      background:var(--white);
      line-height:1.6;
    }
    a{text-decoration:none;color:inherit}
    button,input,select,textarea{font:inherit}
    img{max-width:100%;display:block}

    .topbar{
      background:var(--black);
      color:#fff;
      text-align:center;
      padding:8px 15px;
      font-size:.9rem;
    }

    header{
      position:sticky;
      top:0;
      z-index:1000;
      background:rgba(255,255,255,.96);
      backdrop-filter:blur(12px);
      border-bottom:1px solid var(--border);
    }

    .nav{
      max-width:1200px;
      margin:auto;
      min-height:78px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:0 22px;
    }

    .logo{
      font-family:Georgia,serif;
      font-size:1.8rem;
      font-weight:700;
      letter-spacing:2px;
      color:var(--deep-pink);
    }
    .logo span{color:var(--black)}

    .nav-links{
      display:flex;
      gap:25px;
      align-items:center;
      list-style:none;
    }
    .nav-links a{
      font-weight:600;
      font-size:.95rem;
      transition:.25s;
    }
    .nav-links a:hover{color:var(--pink)}

    .menu-btn{
      display:none;
      border:0;
      background:none;
      font-size:1.8rem;
      cursor:pointer;
    }

    .hero{
      min-height:680px;
      display:grid;
      place-items:center;
      text-align:center;
      padding:90px 20px;
      background:
        linear-gradient(rgba(30,12,22,.56),rgba(30,12,22,.56)),
        url("https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=1800&q=85")
        center/cover;
      color:#fff;
    }

    .hero-content{max-width:850px}
    .eyebrow{
      text-transform:uppercase;
      letter-spacing:4px;
      font-size:.85rem;
      color:#ffd7e8;
      margin-bottom:18px;
      font-weight:700;
    }
    .hero h1{
      font-family:Georgia,serif;
      font-size:clamp(3rem,8vw,6.2rem);
      line-height:1;
      margin-bottom:22px;
    }
    .hero h1 span{color:#ffd0e3}
    .hero p{
      font-size:1.2rem;
      max-width:650px;
      margin:0 auto 32px;
    }

    .buttons{
      display:flex;
      gap:14px;
      justify-content:center;
      flex-wrap:wrap;
    }

    .btn{
      display:inline-flex;
      justify-content:center;
      align-items:center;
      gap:8px;
      padding:13px 23px;
      border-radius:999px;
      border:2px solid var(--pink);
      background:var(--pink);
      color:#fff;
      font-weight:700;
      cursor:pointer;
      transition:.25s;
    }
    .btn:hover{transform:translateY(-2px);background:var(--deep-pink);border-color:var(--deep-pink)}
    .btn-outline{
      background:transparent;
      border-color:#fff;
      color:#fff;
    }
    .btn-outline:hover{background:#fff;color:var(--deep-pink)}

    section{padding:85px 20px}
    .container{max-width:1200px;margin:auto}
    .section-head{text-align:center;max-width:720px;margin:0 auto 45px}
    .section-head .eyebrow{color:var(--pink);margin-bottom:8px}
    .section-head h2{
      font-family:Georgia,serif;
      font-size:clamp(2rem,4vw,3.3rem);
      margin-bottom:12px;
    }
    .section-head p{color:var(--gray)}

    .category-grid,.service-grid,.feature-grid{
      display:grid;
      grid-template-columns:repeat(4,1fr);
      gap:20px;
    }

    .category-card{
      padding:28px;
      background:var(--soft-pink);
      border-radius:var(--radius);
      text-align:center;
      transition:.3s;
      border:1px solid transparent;
    }
    .category-card:hover{
      transform:translateY(-6px);
      border-color:#f1bfd3;
      box-shadow:var(--shadow);
    }
    .icon{font-size:2.2rem;margin-bottom:12px}
    .category-card h3{margin-bottom:7px}
    .category-card p{color:var(--gray);font-size:.94rem}

    .shop-section{background:var(--light)}
    .shop-tools{
      display:flex;
      gap:12px;
      justify-content:space-between;
      align-items:center;
      flex-wrap:wrap;
      margin-bottom:30px;
    }
    .search{
      flex:1;
      min-width:220px;
      padding:13px 17px;
      border:1px solid var(--border);
      border-radius:999px;
      outline:none;
      background:#fff;
    }
    .search:focus{border-color:var(--pink)}
    .filters{display:flex;gap:8px;flex-wrap:wrap}
    .filter{
      border:1px solid var(--border);
      background:#fff;
      padding:10px 15px;
      border-radius:999px;
      cursor:pointer;
    }
    .filter.active,.filter:hover{background:var(--pink);color:#fff;border-color:var(--pink)}

    .product-grid{
      display:grid;
      grid-template-columns:repeat(4,1fr);
      gap:24px;
    }
    .product-card{
      background:#fff;
      border-radius:var(--radius);
      overflow:hidden;
      box-shadow:0 8px 25px rgba(40,15,30,.06);
      border:1px solid var(--border);
      transition:.3s;
    }
    .product-card:hover{transform:translateY(-6px);box-shadow:var(--shadow)}
    .product-image{
      height:270px;
      width:100%;
      object-fit:cover;
      background:#f8eaf1;
    }
    .product-info{padding:20px}
    .tag{
      display:inline-block;
      font-size:.72rem;
      text-transform:uppercase;
      letter-spacing:1px;
      color:var(--deep-pink);
      background:var(--soft-pink);
      padding:5px 9px;
      border-radius:999px;
      margin-bottom:9px;
      font-weight:700;
    }
    .product-info h3{font-family:Georgia,serif;font-size:1.25rem;margin-bottom:4px}
    .product-info p{color:var(--gray);font-size:.9rem;margin-bottom:12px}
    .price{
      font-size:1.15rem;
      font-weight:800;
      margin-bottom:15px;
      color:var(--deep-pink);
    }
    .product-actions{display:flex;gap:8px}
    .product-actions .btn{flex:1;padding:10px 12px;font-size:.85rem}

    .services{
      background:linear-gradient(135deg,#fff,#fff2f7);
    }
    .service-grid{grid-template-columns:repeat(4,1fr)}
    .service-card{
      background:#fff;
      border-radius:var(--radius);
      overflow:hidden;
      border:1px solid var(--border);
      box-shadow:0 8px 25px rgba(40,15,30,.05);
    }
    .service-card img{height:220px;width:100%;object-fit:cover}
    .service-card .content{padding:22px}
    .service-card h3{font-family:Georgia,serif;font-size:1.4rem;margin-bottom:7px}
    .service-card p{color:var(--gray);margin-bottom:16px}

    .about-grid{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:60px;
      align-items:center;
    }
    .about-image{
      border-radius:30px;
      min-height:520px;
      background:url("https://images.unsplash.com/photo-1522335789203-aabd1fc54bc9?auto=format&fit=crop&w=1000&q=85") center/cover;
    }
    .about-copy .eyebrow{text-align:left;color:var(--pink)}
    .about-copy h2{
      font-family:Georgia,serif;
      font-size:clamp(2rem,4vw,3.2rem);
      margin-bottom:18px;
    }
    .about-copy p{color:var(--gray);margin-bottom:18px}
    .values{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:25px}
    .value{padding:16px;background:var(--soft-pink);border-radius:15px;font-weight:700}

    .gallery{background:var(--light)}
    .gallery-grid{
      display:grid;
      grid-template-columns:repeat(4,1fr);
      gap:14px;
    }
    .gallery-grid img{
      width:100%;
      height:250px;
      object-fit:cover;
      border-radius:18px;
      transition:.3s;
    }
    .gallery-grid img:hover{transform:scale(1.02)}

    .booking{
      background:var(--black);
      color:#fff;
    }
    .booking-grid{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:50px;
      align-items:start;
    }
    .booking h2{
      font-family:Georgia,serif;
      font-size:clamp(2.2rem,4vw,3.5rem);
      margin-bottom:15px;
    }
    .booking-copy p{color:#d8ced4;margin-bottom:25px}
    form{display:grid;gap:13px}
    input,select,textarea{
      width:100%;
      padding:14px 16px;
      border-radius:12px;
      border:1px solid #ddd;
      outline:none;
      background:#fff;
      color:var(--black);
    }
    textarea{min-height:120px;resize:vertical}
    form .btn{border:0}

    .contact-grid{
      display:grid;
      grid-template-columns:repeat(3,1fr);
      gap:20px;
      margin-bottom:35px;
    }
    .contact-card{
      padding:28px;
      border:1px solid var(--border);
      border-radius:var(--radius);
      text-align:center;
    }
    .contact-card .icon{font-size:1.8rem}
    .contact-card p{color:var(--gray)}
    .contact-card a{color:var(--deep-pink);font-weight:700}

    .final-cta{
      padding:75px 20px;
      text-align:center;
      background:var(--soft-pink);
    }
    .final-cta h2{font-family:Georgia,serif;font-size:clamp(2rem,4vw,3.4rem);margin-bottom:12px}
    .final-cta p{color:var(--gray);margin-bottom:24px}

    footer{
      background:#110d11;
      color:#fff;
      padding:45px 20px 20px;
    }
    .footer-grid{
      max-width:1200px;
      margin:auto;
      display:grid;
      grid-template-columns:2fr 1fr 1fr;
      gap:40px;
    }
    footer h3{font-family:Georgia,serif;font-size:1.5rem;margin-bottom:12px}
    footer p,footer li{color:#cfc4ca}
    footer ul{list-style:none;display:grid;gap:7px}
    .copyright{
      max-width:1200px;
      margin:35px auto 0;
      padding-top:20px;
      border-top:1px solid #3a3036;
      text-align:center;
      color:#a99ca3;
      font-size:.9rem;
    }

    .floating-wa{
      position:fixed;
      right:22px;
      bottom:22px;
      z-index:900;
      width:58px;
      height:58px;
      border-radius:50%;
      display:grid;
      place-items:center;
      background:#25D366;
      color:#fff;
      font-size:1.55rem;
      box-shadow:0 10px 25px rgba(0,0,0,.2);
    }

    .cart{
      position:fixed;
      right:20px;
      top:95px;
      width:min(380px,calc(100% - 40px));
      background:#fff;
      border-radius:20px;
      box-shadow:0 20px 60px rgba(0,0,0,.2);
      z-index:1200;
      padding:20px;
      display:none;
      border:1px solid var(--border);
    }
    .cart.open{display:block}
    .cart-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:15px}
    .close-cart{border:0;background:none;font-size:1.4rem;cursor:pointer}
    #cartItems{display:grid;gap:10px;max-height:260px;overflow:auto}
    .cart-item{
      display:flex;
      justify-content:space-between;
      gap:10px;
      padding:10px;
      background:var(--soft-pink);
      border-radius:12px;
      font-size:.9rem;
    }
    .cart-total{
      display:flex;
      justify-content:space-between;
      font-weight:800;
      padding:15px 0;
      border-top:1px solid var(--border);
      margin-top:12px;
    }
    .cart-count{
      background:var(--pink);
      color:#fff;
      border-radius:50%;
      padding:2px 7px;
      font-size:.75rem;
      margin-left:4px;
    }

    @media(max-width:1000px){
      .category-grid,.product-grid,.service-grid{grid-template-columns:repeat(2,1fr)}
      .gallery-grid{grid-template-columns:repeat(2,1fr)}
    }

    @media(max-width:760px){
      .nav{min-height:68px}
      .menu-btn{display:block}
      .nav-links{
        position:absolute;
        top:68px;
        left:0;
        right:0;
        background:#fff;
        display:none;
        flex-direction:column;
        padding:22px;
        border-bottom:1px solid var(--border);
      }
      .nav-links.open{display:flex}
      .hero{min-height:620px}
      .about-grid,.booking-grid{grid-template-columns:1fr}
      .about-image{min-height:380px}
      .contact-grid{grid-template-columns:1fr}
      .footer-grid{grid-template-columns:1fr}
    }

    @media(max-width:540px){
      section{padding:65px 16px}
      .category-grid,.product-grid,.service-grid,.gallery-grid{grid-template-columns:1fr}
      .product-image{height:310px}
      .gallery-grid img{height:300px}
      .hero{padding:75px 16px}
      .hero p{font-size:1rem}
      .values{grid-template-columns:1fr}
    }
  </style>
</head>

<body>

  <div class="topbar">
    ✨ Jamiz Glam is open daily from 7:00 AM – 10:00 PM
  </div>

  <header>
    <nav class="nav">
      <a href="#home" class="logo">JAMIZ <span>GLAM</span></a>

      <button class="menu-btn" id="menuBtn" aria-label="Open menu">☰</button>

      <ul class="nav-links" id="navLinks">
        <li><a href="#home">Home</a></li>
        <li><a href="#perfumes">Perfumes</a></li>
        <li><a href="#services">Beauty Services</a></li>
        <li><a href="#gallery">Gallery</a></li>
        <li><a href="#about">About Us</a></li>
        <li><a href="#contact">Contact</a></li>
        <li><a href="#" id="cartButton">🛍️ Cart <span class="cart-count" id="cartCount">0</span></a></li>
      </ul>
    </nav>
  </header>

  <main>

    <section class="hero" id="home">
      <div class="hero-content">
        <div class="eyebrow">Luxury • Beauty • Fragrance</div>
        <h1>Discover Luxury.<br><span>Express Your Elegance.</span></h1>
        <p>
          Welcome to Jamiz Glam — your destination for beautiful makeup,
          bridal glam, elegant styling and luxury fragrances.
        </p>
        <div class="buttons">
          <a href="#perfumes" class="btn">🌸 Explore Perfumes</a>
          <a href="#services" class="btn btn-outline">💄 Explore Beauty Services</a>
        </div>
      </div>
    </section>

    <section>
      <div class="container">
        <div class="section-head">
          <div class="eyebrow">Welcome to Jamiz Glam</div>
          <h2>Beauty Made to Feel Like You</h2>
          <p>Explore our signature fragrance collection and beauty services designed to help you look and feel your best.</p>
        </div>

        <div class="category-grid">
          <div class="category-card">
            <div class="icon">🌸</div>
            <h3>Luxury Perfumes</h3>
            <p>Find a signature scent for every mood and occasion.</p>
          </div>
          <div class="category-card">
            <div class="icon">💄</div>
            <h3>Makeup</h3>
            <p>Professional glam for your special moments.</p>
          </div>
          <div class="category-card">
            <div class="icon">👰</div>
            <h3>Bridal Glam</h3>
            <p>Elegant bridal makeup for your unforgettable day.</p>
          </div>
          <div class="category-card">
            <div class="icon">✨</div>
            <h3>Beauty Styling</h3>
            <p>Henna brows and beautiful turban styling.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="shop-section" id="perfumes">
      <div class="container">
        <div class="section-head">
          <div class="eyebrow">Our Collection</div>
          <h2>Shop Luxury Perfumes</h2>
          <p>Browse our featured fragrance collection. Prices can be updated as your catalogue grows.</p>
        </div>

        <div class="shop-tools">
          <input class="search" id="searchInput" type="search" placeholder="Search for a perfume...">
          <div class="filters">
            <button class="filter active" data-filter="all">All</button>
            <button class="filter" data-filter="women">Women</button>
            <button class="filter" data-filter="men">Men</button>
            <button class="filter" data-filter="unisex">Unisex</button>
          </div>
        </div>

        <div class="product-grid" id="productGrid"></div>
      </div>
    </section>

    <section class="services" id="services">
      <div class="container">
        <div class="section-head">
          <div class="eyebrow">Look Your Best</div>
          <h2>Our Beauty Services</h2>
          <p>From everyday glam to your most important day, Jamiz Glam helps you express your unique beauty.</p>
        </div>

        <div class="service-grid">
          <article class="service-card">
            <img src="https://images.unsplash.com/photo-1487412720507-e7ab37603c6f?auto=format&fit=crop&w=800&q=85" alt="Professional makeup service">
            <div class="content">
              <h3>Makeup</h3>
              <p>Beautiful makeup looks for events, celebrations, photoshoots and special occasions.</p>
              <button class="btn book-service" data-service="Makeup">Book Now</button>
            </div>
          </article>

          <article class="service-card">
            <img src="https://images.unsplash.com/photo-1516975080664-ed2fc6a32937?auto=format&fit=crop&w=800&q=85" alt="Bridal beauty">
            <div class="content">
              <h3>Bridal Makeup</h3>
              <p>Elegant bridal glam designed to make your big day even more unforgettable.</p>
              <button class="btn book-service" data-service="Bridal Makeup">Book Now</button>
            </div>
          </article>

          <article class="service-card">
            <img src="https://images.unsplash.com/photo-1616683693504-3ea7e9ad6fec?auto=format&fit=crop&w=800&q=85" alt="Beauty brows service">
            <div class="content">
              <h3>Henna Brows</h3>
              <p>Beautifully shaped brows that enhance your natural features and confidence.</p>
              <button class="btn book-service" data-service="Henna Brows">Book Now</button>
            </div>
          </article>

          <article class="service-card">
            <img src="https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=800&q=85" alt="Beauty styling">
            <div class="content">
              <h3>Turban Tying</h3>
              <p>Elegant turban styling for celebrations, events and your personal fashion statement.</p>
              <button class="btn book-service" data-service="Turban Tying">Book Now</button>
            </div>
          </article>
        </div>
      </div>
    </section>

    <section id="about">
      <div class="container about-grid">
        <div class="about-image" aria-label="Jamiz Glam beauty image"></div>
        <div class="about-copy">
          <div class="eyebrow">About Jamiz Glam</div>
          <h2>Your Beauty. Your Scent. Your Glam.</h2>
          <p>
            Jamiz Glam is a beauty and fragrance brand dedicated to helping you
            discover your unique elegance. We bring together beauty services,
            creative styling and luxury fragrances in one glamorous experience.
          </p>
          <p>
            Whether you're preparing for your wedding, getting ready for a special
            event, or simply looking for a fragrance that feels like you, we're here
            to help you express your style with confidence.
          </p>

          <div class="values">
            <div class="value">✨ Quality</div>
            <div class="value">💎 Elegance</div>
            <div class="value">❤️ Personal Touch</div>
            <div class="value">🌸 Confidence</div>
          </div>
        </div>
      </div>
    </section>

    <section class="gallery" id="gallery">
      <div class="container">
        <div class="section-head">
          <div class="eyebrow">Our Gallery</div>
          <h2>A Glimpse of the Glam</h2>
          <p>Replace these sample images with your own Jamiz Glam photos as your gallery grows.</p>
        </div>

        <div class="gallery-grid">
          <img src="https://images.unsplash.com/photo-1596462502278-27bfdc403348?auto=format&fit=crop&w=900&q=85" alt="Beauty products">
          <img src="https://images.unsplash.com/photo-1522335789203-aabd1fc54bc9?auto=format&fit=crop&w=900&q=85" alt="Makeup products">
          <img src="https://images.unsplash.com/photo-1487412720507-e7ab37603c6f?auto=format&fit=crop&w=900&q=85" alt="Makeup look">
          <img src="https://images.unsplash.com/photo-1516975080664-ed2fc6a32937?auto=format&fit=crop&w=900&q=85" alt="Beauty styling">
        </div>
      </div>
    </section>

    <section class="booking" id="booking">
      <div class="container booking-grid">
        <div class="booking-copy">
          <div class="eyebrow">Book Your Glam</div>
          <h2>Let's Create Your Perfect Look.</h2>
          <p>
            Send us your booking request and we'll get back to you through WhatsApp
            to confirm availability, pricing and details.
          </p>
          <p><strong>Open daily:</strong> 7:00 AM – 10:00 PM</p>
          <a class="btn" href="https://wa.me/256706536943" target="_blank">💬 Chat on WhatsApp</a>
        </div>

        <form id="bookingForm">
          <input id="customerName" type="text" placeholder="Your full name" required>
          <input id="customerPhone" type="tel" placeholder="Your phone number" required>
          <select id="serviceSelect" required>
            <option value="">Select a service</option>
            <option>Makeup</option>
            <option>Bridal Makeup</option>
            <option>Henna Brows</option>
            <option>Turban Tying</option>
          </select>
          <input id="bookingDate" type="date" required>
          <input id="bookingTime" type="time" required>
          <textarea id="bookingMessage" placeholder="Anything else you'd like us to know?"></textarea>
          <button class="btn" type="submit">📅 Request Appointment via WhatsApp</button>
        </form>
      </div>
    </section>

    <section id="contact">
      <div class="container">
        <div class="section-head">
          <div class="eyebrow">Contact Us</div>
          <h2>We're Here for You</h2>
          <p>For perfume orders, beauty bookings and enquiries, contact Jamiz Glam directly.</p>
        </div>

        <div class="contact-grid">
          <div class="contact-card">
            <div class="icon">📞</div>
            <h3>Call Us</h3>
            <p><a href="tel:+256706536943">+256 706 536 943</a></p>
            <p><a href="tel:+256760686605">+256 760 686 605</a></p>
          </div>
          <div class="contact-card">
            <div class="icon">💬</div>
            <h3>WhatsApp</h3>
            <p>Order perfumes or enquire about services.</p>
            <a href="https://wa.me/256706536943" target="_blank">Start a Chat</a>
          </div>
          <div class="contact-card">
            <div class="icon">🕐</div>
            <h3>Opening Hours</h3>
            <p>Every day</p>
            <strong>7:00 AM – 10:00 PM</strong>
          </div>
        </div>
      </div>
    </section>

    <section class="final-cta">
      <div class="container">
        <h2>Ready to Discover Your Glam?</h2>
        <p>Find your signature scent or book your next beauty experience with Jamiz Glam.</p>
        <div class="buttons">
          <a href="#perfumes" class="btn">🌸 Shop Perfumes</a>
          <a href="#booking" class="btn">📅 Book an Appointment</a>
        </div>
      </div>
    </section>

  </main>

  <a class="floating-wa" href="https://wa.me/256706536943" target="_blank" aria-label="Chat with Jamiz Glam on WhatsApp">💬</a>

  <div class="cart" id="cart">
    <div class="cart-head">
      <h3>Your Perfume Order</h3>
      <button class="close-cart" id="closeCart">✕</button>
    </div>
    <div id="cartItems"></div>
    <div class="cart-total">
      <span>Total</span>
      <span id="cartTotal">$0</span>
    </div>
    <button class="btn" id="sendOrder" style="width:100%">💬 Send Order to WhatsApp</button>
  </div>

  <footer>
    <div class="footer-grid">
      <div>
        <h3>JAMIZ GLAM</h3>
        <p>Discover Luxury. Express Your Elegance.</p>
        <p style="margin-top:10px">Luxury fragrances, makeup, bridal glam and beauty styling.</p>
      </div>
      <div>
        <h3>Quick Links</h3>
        <ul>
          <li><a href="#perfumes">Perfumes</a></li>
          <li><a href="#services">Beauty Services</a></li>
          <li><a href="#gallery">Gallery</a></li>
          <li><a href="#booking">Book Appointment</a></li>
        </ul>
      </div>
      <div>
        <h3>Contact</h3>
        <ul>
          <li><a href="tel:+256706536943">+256 706 536 943</a></li>
          <li><a href="tel:+256760686605">+256 760 686 605</a></li>
          <li>Open daily: 7 AM – 10 PM</li>
        </ul>
      </div>
    </div>
    <div class="copyright">
      © <span id="year"></span> Jamiz Glam. All rights reserved.
    </div>
  </footer>

  <script>
    // ============================================================
    // JAMIZ GLAM PRODUCT CATALOGUE
    // Change the name, category, price and image for each product.
    // Add more products by copying another object below.
    // ============================================================

    const products = [
      {
        id: 1,
        name: "Dior Sauvage",
        category: "men",
        type: "Luxury Fragrance",
        price: 0,
        image: "https://images.unsplash.com/photo-1592945403244-b3fbafd7f539?auto=format&fit=crop&w=800&q=85"
      },
      {
        id: 2,
        name: "YSL Libre",
        category: "women",
        type: "Luxury Fragrance",
        price: 0,
        image: "https://images.unsplash.com/photo-1588405748880-12d1d2a59f75?auto=format&fit=crop&w=800&q=85"
      },
      {
        id: 3,
        name: "Gucci Bloom",
        category: "women",
        type: "Luxury Fragrance",
        price: 0,
        image: "https://images.unsplash.com/photo-1547887538-e3a2f32cb1cc?auto=format&fit=crop&w=800&q=85"
      },
      {
        id: 4,
        name: "Chanel No. 5",
        category: "women",
        type: "Luxury Fragrance",
        price: 0,
        image: "https://images.unsplash.com/photo-1563170351-be82bc888aa4?auto=format&fit=crop&w=800&q=85"
      },
      {
        id: 5,
        name: "Versace Eros",
        category: "men",
        type: "Luxury Fragrance",
        price: 0,
        image: "https://images.unsplash.com/photo-1619994403073-2b4b4f1a3b16?auto=format&fit=crop&w=800&q=85"
      },
      {
        id: 6,
        name: "Lattafa Khamrah",
        category: "unisex",
        type: "Luxury Fragrance",
        price: 0,
        image: "https://images.unsplash.com/photo-1594035910387-fea47794261f?auto=format&fit=crop&w=800&q=85"
      },
      {
        id: 7,
        name: "Club de Nuit Intense Man",
        category: "men",
        type: "Luxury Fragrance",
        price: 0,
        image: "https://images.unsplash.com/photo-1590736969955-71cc94901144?auto=format&fit=crop&w=800&q=85"
      },
      {
        id: 8,
        name: "Signature Rose",
        category: "unisex",
        type: "Jamiz Glam Collection",
        price: 0,
        image: "https://images.unsplash.com/photo-1523293182086-7651a899d37f?auto=format&fit=crop&w=800&q=85"
      }
    ];

    const WHATSAPP_NUMBER = "256706536943";
    let activeFilter = "all";
    let cart = [];

    const productGrid = document.getElementById("productGrid");
    const searchInput = document.getElementById("searchInput");
    const cartEl = document.getElementById("cart");
    const cartItemsEl = document.getElementById("cartItems");
    const cartCountEl = document.getElementById("cartCount");
    const cartTotalEl = document.getElementById("cartTotal");

    function money(price){
      return price > 0 ? "$" + price.toLocaleString() : "Price on Request";
    }

    function renderProducts(){
      const search = searchInput.value.toLowerCase().trim();

      const filtered = products.filter(p => {
        const matchesFilter = activeFilter === "all" || p.category === activeFilter;
        const matchesSearch =
          p.name.toLowerCase().includes(search) ||
          p.type.toLowerCase().includes(search);
        return matchesFilter && matchesSearch;
      });

      if(filtered.length === 0){
        productGrid.innerHTML = "<p style='grid-column:1/-1;text-align:center;color:#6f6870'>No perfumes found. Try another search.</p>";
        return;
      }

      productGrid.innerHTML = filtered.map(p => `
        <article class="product-card">
          <img class="product-image" src="${p.image}" alt="${p.name}" loading="lazy">
          <div class="product-info">
            <span class="tag">${p.category}</span>
            <h3>${p.name}</h3>
            <p>${p.type}</p>
            <div class="price">${money(p.price)}</div>
            <div class="product-actions">
              <button class="btn add-cart" data-id="${p.id}">🛍️ Add</button>
              <button class="btn" onclick="orderSingle(${p.id})">💬 Order</button>
            </div>
          </div>
        </article>
      `).join("");

      document.querySelectorAll(".add-cart").forEach(btn => {
        btn.addEventListener("click", () => addToCart(Number(btn.dataset.id)));
      });
    }

    function orderSingle(id){
      const p = products.find(x => x.id === id);
      const message =
        `Hello Jamiz Glam!%0A%0A` +
        `I am interested in ordering:%0A` +
        `*${encodeURIComponent(p.name)}*%0A` +
        `Price: ${encodeURIComponent(money(p.price))}%0A%0A` +
        `Please let me know about availability. Thank you!`;

      window.open(`https://wa.me/${WHATSAPP_NUMBER}?text=${message}`, "_blank");
    }

    function addToCart(id){
      const p = products.find(x => x.id === id);
      cart.push(p);
      renderCart();
      cartEl.classList.add("open");
    }

    function removeFromCart(index){
      cart.splice(index,1);
      renderCart();
    }

    function renderCart(){
      cartCountEl.textContent = cart.length;

      if(cart.length === 0){
        cartItemsEl.innerHTML = "<p style='color:#6f6870'>Your order list is empty.</p>";
      }else{
        cartItemsEl.innerHTML = cart.map((p,i) => `
          <div class="cart-item">
            <span>${p.name}</span>
            <button onclick="removeFromCart(${i})" style="border:0;background:none;cursor:pointer">✕</button>
          </div>
        `).join("");
      }

      const total = cart.reduce((sum,p) => sum + (Number(p.price) || 0), 0);
      cartTotalEl.textContent = total > 0 ? "$" + total.toLocaleString() : "Price on Request";
    }

    document.querySelectorAll(".filter").forEach(btn => {
      btn.addEventListener("click", () => {
        document.querySelectorAll(".filter").forEach(b => b.classList.remove("active"));
        btn.classList.add("active");
        activeFilter = btn.dataset.filter;
        renderProducts();
      });
    });

    searchInput.addEventListener("input", renderProducts);

    document.getElementById("cartButton").addEventListener("click", e => {
      e.preventDefault();
      cartEl.classList.toggle("open");
    });

    document.getElementById("closeCart").addEventListener("click", () => {
      cartEl.classList.remove("open");
    });

    document.getElementById("sendOrder").addEventListener("click", () => {
      if(cart.length === 0){
        alert("Please add at least one perfume to your order.");
        return;
      }

      const items = cart.map(p => `- ${p.name} (${money(p.price)})`).join("%0A");
      const message =
        `Hello Jamiz Glam!%0A%0A` +
        `I would like to order:%0A${items}%0A%0A` +
        `Please confirm availability and the final price. Thank you!`;

      window.open(`https://wa.me/${WHATSAPP_NUMBER}?text=${message}`, "_blank");
    });

    document.querySelectorAll(".book-service").forEach(btn => {
      btn.addEventListener("click", () => {
        document.getElementById("serviceSelect").value = btn.dataset.service;
        document.getElementById("booking").scrollIntoView({behavior:"smooth"});
      });
    });

    document.getElementById("bookingForm").addEventListener("submit", e => {
      e.preventDefault();

      const name = document.getElementById("customerName").value;
      const phone = document.getElementById("customerPhone").value;
      const service = document.getElementById("serviceSelect").value;
      const date = document.getElementById("bookingDate").value;
      const time = document.getElementById("bookingTime").value;
      const message = document.getElementById("bookingMessage").value;

      const text =
        `Hello Jamiz Glam!%0A%0A` +
        `I would like to request a booking.%0A%0A` +
        `Name: ${encodeURIComponent(name)}%0A` +
        `Phone: ${encodeURIComponent(phone)}%0A` +
        `Service: ${encodeURIComponent(service)}%0A` +
        `Preferred Date: ${encodeURIComponent(date)}%0A` +
        `Preferred Time: ${encodeURIComponent(time)}%0A` +
        `Message: ${encodeURIComponent(message || "None")}`;

      window.open(`https://wa.me/${WHATSAPP_NUMBER}?text=${text}`, "_blank");
    });

    const menuBtn = document.getElementById("menuBtn");
    const navLinks = document.getElementById("navLinks");

    menuBtn.addEventListener("click", () => {
      navLinks.classList.toggle("open");
    });

    document.querySelectorAll(".nav-links a").forEach(link => {
      link.addEventListener("click", () => navLinks.classList.remove("open"));
    });

    document.getElementById("year").textContent = new Date().getFullYear();

    renderProducts();
    renderCart();
  </script>
</body>
</html>

